<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console Output with Colors for Local Development -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %highlight(%-5level) %cyan(%logger{36}) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Advanced Size and Time Based Rolling File Appender for Production -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Active log file -->
        <file>logs/application.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Archives daily, indexes multiple files per day (%i), and compresses to .gz to save 90% disk space -->
            <fileNamePattern>logs/application-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>

            <!-- Max size per individual active file before it gets zipped and a new one starts -->
            <maxFileSize>10MB</maxFileSize>

            <!-- Maximum number of days to keep historical logs -->
            <maxHistory>30</maxHistory>

            <!-- Absolute hard cap: The entire logs folder will NEVER exceed 1GB -->
            <totalSizeCap>1GB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root Logger Configuration -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>
</configuration>